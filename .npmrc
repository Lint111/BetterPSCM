# Cross-platform binaries are handled via optionalDependencies in package.json.
# Both Linux (WSL) and Windows variants are listed so npm install from either
# platform produces a working node_modules for both.
